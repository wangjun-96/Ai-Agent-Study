# 项目初始化提示

初始化一个名为'{{agent-frontend-project}}'的新项目。


**项目配置：**


- **框架：** ‘{{Vue3}}’
- **语言：** ‘{{TypeScript}}’
- **包管理器：** ‘{{npm}}’
- **UI 框架：** ‘{{Element Plus}}’
- **状态管理：** ‘{{Pinia}}’
- **构建工具：** ‘{{Vite}}’

**操作指令：**
1. 创建项目目录。
2. 使用指定框架和语言初始化项目。
3. 安装并配置所有必要的依赖项，包括 UI 框架。
4. 配置代码检查和格式化工具。
5. 确保项目可以成功构建和运行。