# 测试环境配置
# NODE_ENV 由 Vite 按 mode 自动注入，请勿在此手动设置

# 应用标题
VITE_APP_TITLE=Agent Frontend (Test)

# 接口请求前缀
VITE_API_PREFIX=/api

# 跨域代理目标地址（测试环境后端服务地址）
VITE_PROXY_TARGET=https://test-api.example.com

# 接口基础路径（axios 实例使用；测试环境走代理同源转发，置空即可）
VITE_API_BASE_URL=

# 是否开启调试日志
VITE_LOG_ENABLED=true
